# Cross-layer tests

The client and server each have smoke tests. The intended next test is an end-to-end Playwright flow: load the client, enter `passport`, submit, and assert the result heading appears.
